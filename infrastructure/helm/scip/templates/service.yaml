apiVersion: v1
kind: Service
metadata:
  name: {{ include "scip.fullname" . }}
  labels:
    {{- include "scip.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "scip.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
